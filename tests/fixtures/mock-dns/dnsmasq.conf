# RunSecure — Mock DNS server config (test fixture)
# Serves test-fixture DNS entries without forwarding to any real upstream.

no-resolv
no-hosts
listen-address=0.0.0.0
port=53
log-queries
addn-hosts=/etc/mock-dns-hosts.txt

# Return NXDOMAIN for anything not in our hosts file
